import { StyleSheet } from "react-native"

export const style = StyleSheet.create({
    container: {
        flexWrap: 'wrap',
        flexDirection: 'row',
        paddingBottom: 200,
        gap: 12
    }
})